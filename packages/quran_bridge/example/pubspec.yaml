name: quran_bridge_example
description: Demonstrates quran_bridge plugin usage.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  quran_bridge:
    path: ../
